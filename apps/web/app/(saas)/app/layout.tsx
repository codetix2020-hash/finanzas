import type { PropsWithChildren } from "react";

// AUTENTICACIÓN COMPLETAMENTE DESHABILITADA
// Todas las validaciones eliminadas
export default function Layout({ children }: PropsWithChildren) {
	return <>{children}</>;
}
